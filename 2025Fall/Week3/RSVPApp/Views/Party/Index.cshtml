@using RSVPApp.Models
@model List<Guest>

<h3>Who's coming:</h3>
<table>
    <tbody>
        @foreach(Guest i in Model){
        <tr>
            @if(i.WillAttend){
                <td> @i.Name </td>
            }
        </tr>
        }
    </tbody>
</table>