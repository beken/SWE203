@{
    Layout = "_Layout";
}

@* If we are using _Layout for each view, instead of adding layout to each view separately,
we can just create a _ViewStart file with that uses _Layout. 
_ViewStart is a special page that adds _Layout to each view. *@